딜리칩 데이터 추출 작업을 수행해줘.

== 목표 ==
지정한 폴더 이미지를 분석하여 지정한 JSON 파일을 최신 데이터로 덮어써.
기존 웹사이트 카드 UI 코드는 절대 수정하지 않는다.

== 공통 원칙 ==
1. 화면에 명시적으로 표시된 텍스트·숫자만 추출. 임의 계산·추론 금지.
2. 쿠폰·프로모션 카드만 추출. 매장 리스트·리뷰·광고·추천 섹션은 완전히 무시.
3. "받기 완료" "종료" "마감" 상태 쿠폰은 제외.
4. 동일 브랜드 이미지가 여러 장(예: 금액 화면 + 기간 화면)이면 하나의 레코드로 합쳐서 처리.
5. 한 이미지에 여러 카드가 있으면 상단→하단 순서로 전부 추출.

== 공통 필드 규칙 ==
- app: BM→"배달의민족" / COUP→"쿠팡이츠" / YO→"요기요" / TTANG→"땡겨요"
- brand: 순수 브랜드명만. "할인" "최대" "이번주" "쿠폰" 등 수식어 제거.
- discount: 숫자만.
  (예: "4,000원 할인" → 4000)
  % 할인 쿠폰은 최대 할인 금액을 숫자로 기록.
  (예: "10% 최대할인금액 1,000원" → 1000,
       "10%" 정보는 special_condition에 "10%할인 최대 1,000원" 형식으로 기록)
  최대 금액 명시 없이 %만 있으면 해당 레코드 제외.
- min_order: 숫자만. 명시 없으면 null.
- method: "배달" | "픽업" | "전체". 불명확하면 "전체".
- delivery_types: 명시된 태그 배열. 없으면 [].
- special_condition: 부가 조건 텍스트. 없으면 null.
- valid_until: YYYY-MM-DD 형식. (예: 4월 15일→2026-04-15, 없으면 null)

== JSON 출력 형식(필수) ==
[
  {
    "app": "배달의민족",
    "brand": "브랜드명",
    "discount": 4000,
    "min_order": 15000,
    "method": "전체",
    "delivery_types": ["한집배달"],
    "special_condition": null,
    "valid_until": "2026-04-15"
  }
]

== 새 브랜드 처리(필수) ==
추출된 브랜드 중 scripts/sync-all-data.js의 categoryMap에 없는 브랜드가 있으면:
1. 해당 브랜드 업종을 파악하여 아래 카테고리 중 하나로 분류.
   chicken / pizza / burger / korean / bunsik / meat / chinese / japanese / western / cafe / bakery
2. sync-all-data.js의 categoryMap 해당 섹션에 직접 추가.

== 품질 검증(필수) ==
추출 완료 후 확인:
- app / brand / discount 중 하나라도 비어있는 레코드는 배열에서 제외.
- 카드 수가 0이거나 비정상적으로 적으면 해당 이미지 재분석.
- 폴더별 보고: 이미지 수 / 총 추출 카드 수 / 이미지별 카드 수 (파일명: n개).

중요:
- 이번 실행에서는 지정된 폴더/JSON만 처리하고, 다른 앱 폴더/JSON은 수정하지 마.
- 출력 키 순서/타입/null 규칙을 위 형식과 동일하게 유지해.

---

[공통 베이스 프롬프트를 먼저 적용한 뒤 아래를 추가로 적용]

== 이번 실행 대상 ==
- 입력 폴더: BM
- 출력 파일: BM.json

== 배달의민족(BM) 추출 규칙 ==
- 일반 쿠폰, 선착순 쿠폰, 브랜드찜 쿠폰, 첫 주문 쿠폰 모두 포함.
- 브랜드찜 쿠폰: special_condition에 "브랜드찜 전용" 표기.
- 첫 주문 쿠폰: special_condition에 "첫 주문 전용" 표기.
- 선착순 쿠폰: 유효기간이 당일이어도 유효 쿠폰으로 포함. special_condition에 반드시 "매일 10:00 선착순" 기입.
- 선착순 쿠폰의 valid_until: 해당 브랜드의 다른 일반 쿠폰과 동일한 날짜로 기입. 다른 쿠폰이 없으면 null.
- delivery_types: 카드에 명시된 배달 방식 태그 그대로 배열로 기록.
  (예: "한집배달", "알뜰배달", "포장", "가게배달")
  명시 없으면 [] 처리, method는 "전체".

== 실행 완료 후 보고 ==
- BM 최종 카드 수
- 신규 추가 브랜드 및 배정 카테고리 내역(있을 때)
- BM 폴더 기준: 이미지 수 / 총 추출 카드 수 / 이미지별 카드 수

---

[공통 베이스 프롬프트를 먼저 적용한 뒤 아래를 추가로 적용]

== 이번 실행 대상 ==
- 입력 폴더: YO
- 출력 파일: YO.json

== 요기요(YO) 추출 규칙 ==
- 기본 할인액과 추가 할인액이 별도로 있으면 각각 개별 레코드로 추출.
- % 할인:
  - discount = 최대 할인 금액(숫자만)
  - special_condition에 "X%할인 최대 N원" 형식으로 기록. (예: "10%할인 최대 3,000원")
- 적립 혜택(요기패스 N% 적립): special_condition에 "요기패스 N% 적립" 형식으로 추가.
  기본 할인과 적립이 함께 있으면 " / "로 구분하여 병기.
  (예: "10%할인 최대 3,000원 / 요기패스 2% 적립")
- delivery_types: 카드에 명시된 태그 그대로 기록. 없으면 [].

== 실행 완료 후 보고 ==
- YO 최종 카드 수
- 신규 추가 브랜드 및 배정 카테고리 내역(있을 때)
- YO 폴더 기준: 이미지 수 / 총 추출 카드 수 / 이미지별 카드 수

---

[공통 베이스 프롬프트를 먼저 적용한 뒤 아래를 추가로 적용]

== 이번 실행 대상 ==
- 입력 폴더: COUP
- 출력 파일: COUP.json

== 쿠팡이츠(COUP) 추출 규칙 ==

■ 목록/브랜드컬렉션 페이지 이미지:
  - "최대 XX원"으로 표시된 브랜드 → 해당 이미지에서 추출하지 말 것.
    (같은 폴더에 해당 브랜드의 상세 페이지 캡처가 별도로 있음)
  - 고정 금액(예: "5,000원")으로 표시된 브랜드 → 해당 금액 그대로 추출.

■ 가게 상세 쿠폰 페이지(혜택 모아보기) 이미지:
  - 이 이미지가 있으면 해당 브랜드는 이 페이지 기준으로만 추출.
  - "중복 가능한 할인" 섹션의 쿠폰 카드를 각각 별도 레코드로 추출.
  - 쿠폰 카드에 "배달" 태그 → method: "배달"
  - 쿠폰 카드에 "포장" 태그 → method: "픽업"
  - "X원 이상 주문 시" → min_order에 해당 금액 기록.
  - "와우회원 배달비 0원" 혜택 → delivery_types에 "배달비 0원" 추가 (별도 레코드 X).
  - "즉시할인" → 별도 레코드로 추출 (special_condition: "즉시할인").

■ 기간 표시 변환:
  - "X월 N째주까지" → 해당 월의 N번째 일요일로 변환.
    예) "4월 셋째주까지" → 2026-04-20
  - "YYYY/MM/DD까지" → YYYY-MM-DD 형식 변환.
  - 없으면 null.

■ 기타:
  - 랜덤박스: 열어서 나온 최솟값 기준.
  - 선착순: special_condition에 "선착순 09:00부터" (시간 미표시 기본값).
  - delivery_types: 명시된 문구 그대로. 없으면 [].

== 실행 완료 후 보고 ==
- COUP 최종 카드 수
- 신규 추가 브랜드 및 배정 카테고리 내역(있을 때)
- COUP 폴더 기준: 이미지 수 / 총 추출 카드 수 / 이미지별 카드 수

---

[공통 베이스 프롬프트를 먼저 적용한 뒤 아래를 추가로 적용]

== 이번 실행 대상 ==
- 입력 폴더: TTANG
- 출력 파일: TTANG.json

== 땡겨요(TTANG) 추출 규칙 ==
- 화면에 표시된 최대 할인액을 그대로 discount에 기록.
  (5,000원 차감 계산 절대 금지 — 스크립트가 자동 처리함)
- delivery_types: 없으면 [].

== 실행 완료 후 보고 ==
- TTANG 최종 카드 수
- 신규 추가 브랜드 및 배정 카테고리 내역(있을 때)
- TTANG 폴더 기준: 이미지 수 / 총 추출 카드 수 / 이미지별 카드 수

---

BM/YO/COUP/TTANG 추출이 모두 완료되었다.
아래를 순서대로 실행해:

1) node scripts/sync-all-data.js
2) node scripts/upload-to-supabase.js
3) git add -A && git commit -m "data: 쿠폰 데이터 업데이트" && git push origin main

== 최종 보고 형식 ==
- BM / YO / COUP / TTANG 각각 최종 카드 수
- 신규 추가 브랜드 및 배정 카테고리 내역
- 병합 총 아이템 수
- Supabase 업로드 성공/실패 여부
- 실패 시 원인 및 수정 내역
